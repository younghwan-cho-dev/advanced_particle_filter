# Core (CPU library, classical filtering thread)
numpy>=1.21
scipy>=1.7
matplotlib>=3.4

# TensorFlow side (DPF, HMC, amortized OT)
# Tested with TF 2.16, TFP 0.24
tensorflow>=2.12
tensorflow-probability>=0.20

# Notebook execution
jupyter
ipykernel

# Tests
pytest

# Optional: faster parallel computation in NumPy filters
numba
